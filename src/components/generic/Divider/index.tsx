import type { FC } from 'react';
import styles from '@Components/generic/Divider/Divider.module.css';

const Divider: FC = () => {
  return <div className={styles.divider__container}></div>;
};

export default Divider;
